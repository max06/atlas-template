name: ATLAS Snapshot Review
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  review:
    uses: max06/atlas/.github/workflows/snapshot-review.yml@main
    with:
      helmfile-path: helmfile.yaml.gotmpl
    # Uncomment if you use SOPS-encrypted value files:
    # secrets:
    #   sops-age-key: ${{ secrets.SOPS_AGE_KEY }}
